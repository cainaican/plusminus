import { SixteenDigitWithSpacesDirective } from './sixteen-digit-with-spaces.directive';

describe('ExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new SixteenDigitWithSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
